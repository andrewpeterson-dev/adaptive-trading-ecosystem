[build]
builder = "railpack"

[deploy]
startCommand = "sh -c \"alembic upgrade head && uvicorn api.main:app --host 0.0.0.0 --port ${PORT:-8000}\""
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
